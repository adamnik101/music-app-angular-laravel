import { FeaturesIdPipe } from './features-id.pipe';

describe('FeaturesIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturesIdPipe();
    expect(pipe).toBeTruthy();
  });
});
